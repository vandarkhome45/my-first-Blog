
# 个人博客项目开发过程审视报告

## 一、项目概况

本项目是一个基于 Spring Boot 的个人博客系统，采用 Java 25 和 Spring Boot 3.5.12 构建，使用 MySQL 作为数据库，JPA 作为 ORM 框架。项目以领域驱动设计（DDD）思想为指导，采用模块化架构组织代码。

| 维度 | 详情 |
|------|------|
| 框架 | Spring Boot 3.5.12 |
| Java 版本 | 25 |
| 数据库 | MySQL + Spring Data JPA |
| 认证 | Spring Security + JWT (jjwt 0.11.5) |
| Markdown | commonmark 0.21.0 (含 GFM 表格和自动链接扩展) |
| 构建工具 | Maven |
| Java 源文件 | 31 个 |
| 代码总行数 | 约 4,029 行 |
| 开发周期 | 2026年3月30日 - 4月1日（3天） |
| Git 提交数 | 20 次 |

## 二、项目架构分析

### 2.1 模块化设计

项目使用了基于业务领域的模块化架构，共包含 8 个功能模块和公共组件：

**已完成并存在于源代码中的模块：**

- **Blog 模块** (module/blog/)：博客文章 CRUD，含 Entity、Repository、Service、Controller 完整四层，支持分页查询、按分类和标签筛选，203 行
- **Search 模块** (module/search/)：全文搜索、高级搜索（按标题/内容/全部范围）、关键词高亮、搜索建议和热门搜索，Service 155 行 + Controller 177 行
- **Markdown 模块** (module/markdown/)：Markdown 转 HTML 渲染、摘要提取、代码块检测，含 MarkdownUtil 工具类 (157 行)、MarkdownService (147 行)、MarkdownController (216 行)
- **User 模块** (module/user/)：用户注册/登录、JWT 认证、个人资料管理、密码修改，含 Entity (235 行)、Service (222 行)、Controller (244 行)、JwtUtil (173 行)、JwtAuthenticationFilter (78 行)
- **Upload 模块** (module/upload/)：图片/文档/任意文件上传、批量上传、文件删除，Service 280 行 + Controller 182 行
- **Archive 模块** (module/archive/)：按年月归档、时间线展示、最新/随机推荐文章、归档统计，Service 191 行 + Controller 128 行
- **Statistics 模块** (module/statistics/)：阅读量计数、点赞/取消点赞、热门文章排行、全站统计，Service 225 行 + Controller 174 行

**公共组件：**

- `Result<T>`：统一 API 响应封装（51 行）
- `PageResult<T>`：分页结果封装（15 行）
- `GlobalExceptionHandler`：全局异常处理（21 行）
- `ModuleConfig`：模块开关配置读取（87 行）
- `ModuleInterceptor`：模块启用检查拦截器（43 行）
- `SecurityConfig`：Spring Security 配置（73 行）
- `WebConfig`：Web 拦截器注册（55 行）

### 2.2 架构评价

**优点：**

1. 模块化设计理念清晰。按业务领域而非技术层划分代码，与 MODULE_ARCHITECTURE.txt 中描述的设计理念一致，这在实际项目中是推荐的做法
2. 代码分层规范。大部分模块遵循 Entity → Repository → Service → Controller 四层架构，层次分明
3. 模块开关机制。通过 `module-config.properties` 和 `ModuleInterceptor` 实现了模块的启用/禁用控制，这是一个实用的设计
4. 统一响应格式。`Result<T>` 泛型封装保证了 API 响应的一致性

**待改进：**

1. Comment 模块实现不完整。源代码中只存在 `module/comment/dto/CommentDTO.java`，而 `CommentController`、`CommentService`、`CommentRepository` 在源码目录中缺失。Comment 实体类（`entity/Comment.java`）被放置在项目根级别的 entity 包下，而不是 `module/comment/entity/`，与文档中描述的模块化架构不一致
2. Comment 实体的包路径为 `com.example.blog.entity`，但文档声称它应该在 `module/comment/entity/` 下，实际位置与架构文档不符

## 三、开发过程分析（基于 Git 历史）

### 3.1 开发时间线

```
2026-03-30 19:51-21:51  初始阶段：6次"初始提交：博客系统基础框架"（存在大量重复提交）
2026-03-31 19:12         评论系统开发
2026-03-31 19:17         重新执行初始提交（覆盖之前的工作）
2026-03-31 19:40-19:54   评论系统修改和完善
2026-03-31 20:12-20:15   AI代码项目全优化版（两次提交）
2026-03-31 20:25-20:26   文章检索功能 + 指导文档
2026-03-31 20:30-20:39   文件上传功能开发与优化
2026-03-31 20:45         用户登录注册功能
2026-03-31 20:50         项目优化
2026-03-31 20:55         Markdown支持
2026-04-01 15:33         修改报错（BUG修复）
```

### 3.2 Git 历史问题

1. **重复初始提交**：存在 6 次内容高度重复的"初始提交：博客系统基础框架"，表明版本控制使用不够规范。项目根目录下的 `cleanup-git-history.sh` 脚本就是为解决此问题而编写的，但未被实际执行
2. **提交信息不精确**："AI代码项目全优化版"、"项目优化"等提交信息无法说明具体变更内容
3. **时间线异常**：3月30日晚上有多次提交，而3月31日从19:12开始密集提交——暗示中间的白天时间可能进行了大量代码编写，但在同一天晚上集中提交
4. **Git 仓库设计**：采用本地裸仓库（`/Users/gao_apple/Desktop/blog-system/blog.git`）作为远程仓库，对于个人项目来说是可行的简化方案

### 3.3 开发模式特征

从提交历史和代码特征来看，该项目的开发模式呈现以下特征：

1. **迭代式功能追加**：评论系统 → 搜索 → AI优化 → 文档 → 文件上传 → 用户认证 → Markdown → BUG修复，每个新功能以独立的 Git 提交追加
2. **AI 辅助开发**：存在"AI代码项目全优化版"提交，暗示使用了 AI 工具辅助代码编写和优化
3. **文档驱动**：项目包含三份详尽的中文文档（MODULE_ARCHITECTURE.txt 331 行、MODULE_INDEX.txt 330 行、QUICK_REFERENCE.txt 138 行），这在个人项目中较为少见，说明开发者重视可维护性
4. **Shell 脚本自动化**：编写了多个 Shell 脚本（`git-init-steps.sh`、`init-git-repo.sh`、`commit-comment-module.sh`、`commit-modular-refactor.sh`、`test-modules.sh`、`test-upload.sh` 等）来自动化 Git 操作和测试流程

## 四、代码质量评估

### 4.1 优点

1. **中文注释规范**。所有类和方法都带有详细的中文 Javadoc 注释，注释质量较高
2. **JPA 生命周期管理**。BlogEntity 和 User 实体使用了 `@PrePersist` 和 `@PreUpdate` 回调自动处理时间戳和默认值
3. **安全性意识**。User 模块的 `sanitizeUser()` 方法在返回用户信息前去除密码字段，JWT 过滤器正确配置了无状态会话管理
4. **参数校验**。Controller 层进行了基本的参数校验（如用户名不能为空、密码长度检查）
5. **异常处理**。全局异常处理器统一处理 `BlogException` 和通用异常，返回一致的错误格式
6. **Markdown 支持完善**。使用 commonmark 库并启用了 GFM 表格和自动链接扩展，还提供了摘要提取、代码块检测等实用功能

### 4.2 需要关注的问题

**安全性：**

1. 数据库密码 `114514` 明文存储在 `application.properties` 中，这是一个明显的安全隐患
2. JWT 密钥 `MyBlogSystemSecretKey2024VeryLongSecretKeyForHS256` 同样明文存储，且密钥值较为简单
3. SecurityConfig 中 `.anyRequest().permitAll()` 配置意味着所有请求无需认证即可访问，虽然注释说明是"暂时全部开放"，但这削弱了引入 Spring Security 的意义

**代码质量：**

4. `ModuleConfig.checkCommentModule()` 是一个静态方法，但直接硬编码返回 `true`，没有实际调用配置值，属于死代码
5. Comment 实体（`entity/Comment.java`）缺少 `@PrePersist` 生命周期回调，与 BlogEntity 和 User 实体的实现风格不一致
6. `PageResult.java` 缺少 getter/setter 方法，虽然字段是包级私有，可能影响了序列化

**项目配置：**

7. `.gitignore` 中包含 `HELP.md` 的忽略规则，但根目录下同时存在该文件（Spring Initializr 生成），说明该规则未被正确应用
8. `.DS_Store` 和 `.idea` 目录被提交到了 Git 仓库中（`.gitignore` 中有 `.idea` 规则但可能是在提交之后才添加的）

## 五、文档与测试

### 5.1 文档体系

项目拥有超过 1,000 行的中文技术文档，这在个人项目中非常突出：

- **MODULE_ARCHITECTURE.txt**（250 行）：详细解释了模块化设计理念、各模块功能、开发指南和最佳实践
- **MODULE_INDEX.txt**（330 行）：快速定位指南，按场景说明应修改哪些文件
- **QUICK_REFERENCE.txt**（138 行）：修改速查表和 IDEA 快捷键参考

这些文档内容详实、结构清晰，对于后续维护者（包括未来的自己）非常有价值。

### 5.2 测试情况

- **单元测试**：`src/test/java` 目录未发现任何 Java 测试类（项目中不存在 JUnit/Spring Boot Test 编写的自动化单元测试）
- **HTTP 接口测试**：`src/test/resources/` 目录下有 8 个 `.http` 文件，覆盖了 blog、comment、search、user、upload、archive、statistics、markdown 模块，使用 IntelliJ IDEA 的 HTTP Client 格式
- **Shell 集成测试**：`test-modules.sh` 和 `test-upload.sh` 提供了基于 curl 的端到端测试脚本，覆盖了博客 CRUD、评论、搜索、文件上传等核心功能

总体而言，测试依赖手动执行的 HTTP 请求文件和 Shell 脚本，缺少可自动化的单元测试和集成测试。

## 六、开发过程总结

### 6.1 值得肯定的地方

1. **架构意识强**。在个人项目中采用模块化 DDD 架构，说明开发者对软件工程有较好的理解
2. **文档习惯好**。三条核心文档 + 代码注释，形成了完整的知识体系
3. **自动化意识**。编写了多个 Shell 脚本来辅助 Git 操作和测试
4. **技术选型合理**。Spring Boot + JPA + JWT + Markdown 的技术组合是构建博客系统的成熟方案
5. **功能完整度高**。3 天内完成了从博客核心到评论、搜索、认证、文件上传、归档、统计共 8 个模块的开发

### 6.2 可改进的地方

1. **Git 使用规范性**。应避免重复提交和模糊的提交信息。建议采用 Conventional Commits 规范（如 `feat:`, `fix:`, `refactor:`），部分提交已经采用了类似格式，可以全面推广
2. **代码与文档一致性**。Comment 模块的代码位置与架构文档描述不一致，后续修改时需同步更新文档
3. **敏感信息管理**。数据库密码和 JWT 密钥应使用环境变量或外部配置文件管理，而非直接硬编码
4. **测试体系**。建议补充 JUnit 单元测试和 Spring Boot 集成测试，让测试可自动化运行
5. **配置分离**。建议创建 `application-dev.properties` 和 `application-prod.properties`，实现开发与生产环境的配置隔离

### 6.3 项目成熟度评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整性 | ⭐⭐⭐⭐ | 8 个模块覆盖了博客系统的核心需求 |
| 代码质量 | ⭐⭐⭐ | 整体良好，部分安全配置和死代码需改进 |
| 架构设计 | ⭐⭐⭐⭐ | 模块化 DDD 设计合理，模块开关机制实用 |
| 文档完善度 | ⭐⭐⭐⭐⭐ | 文档非常详尽，远超一般个人项目水平 |
| 测试覆盖 | ⭐⭐ | 缺少自动化测试，依赖手动 HTTP 测试 |
| Git 规范 | ⭐⭐ | 存在重复提交，提交信息不够精确 |
| 安全性 | ⭐⭐ | 敏感信息硬编码，安全配置过于宽松 |

## 七、建议后续行动

1. 执行 `cleanup-git-history.sh` 清理重复的初始提交，重整 Git 历史
2. 将数据库密码和 JWT 密钥迁移到环境变量或 `application-secret.properties`（加入 `.gitignore`）
3. 补全 Comment 模块的 Controller、Service、Repository（已在文档中设计但源码中缺失）
4. 为 BlogService 和 UserService 编写 JUnit 单元测试
5. 收紧 SecurityConfig 的权限配置，对管理类接口（如删除博客、删除评论）添加认证要求
6. 将 Comment 实体迁移到 `module/comment/entity/` 包下以保持架构一致性
7. 建议在后续功能迭代中引入 Swagger/OpenAPI 文档自动生成
